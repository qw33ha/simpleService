apiVersion: v1
kind: Service
metadata:
  name: simple-service
  namespace: nl2service
spec:
  type: ClusterIP
  selector:
    app: simple-service
  ports:
    - name: http
      port: 80
      targetPort: 8080