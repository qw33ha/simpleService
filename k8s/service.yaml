apiVersion: v1
kind: Service
metadata:
  name: simpleService
  namespace: nl2service
spec:
  type: ClusterIP
  selector:
    app: simpleService
  ports:
    - name: http
      port: 80
      targetPort: 8080